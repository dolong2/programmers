class Solution {
    fun solution(array: IntArray, commands: Array<IntArray>): IntArray {
        var answer = intArrayOf()
        var temp = intArrayOf()
        for(i in commands){
            temp = array
        }
        return answer
    }
}